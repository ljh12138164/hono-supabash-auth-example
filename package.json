{
  "name": "supabase-auth",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "pnpm run build:supabase && turbo --filter=supabase dev",
    "build:supabase": "turbo --filter=@hono/supabash-auth build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
